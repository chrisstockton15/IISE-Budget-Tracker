wb = xlsx_package.workbook

wb.add_worksheet(name: "iise_budget_tracker") do |sheet|
  # Create the header row
  sheet.add_row ["Price"]
  # Create entries for each item
  @budget_request.each do |item|
    sheet.add_row [budget_request.totalPrice]
  end
end